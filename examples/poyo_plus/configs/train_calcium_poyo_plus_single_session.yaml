defaults:
  - defaults.yaml
  - model: calcium_poyo_plus_single_session.yaml
  - dataset: calcium_poyo_plus_single_session.yaml
  - _self_  # ensures current config overrides defaults

epochs: 100
batch_size: 64

train_transforms:
  - _target_: torch_brain.transforms.UnitDropout
    field: "calcium_traces.df_over_f"
    min_units: 10
    mode_units: 50
    max_units: 300
    tail_right: 100

wandb:
  run_name: calcium_poyo_plus_single_session
  